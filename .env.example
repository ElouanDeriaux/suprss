# Configuration SUPRSS
# Copiez ce fichier vers .env et remplissez les valeurs

# Clé secrète pour JWT (générez une clé sécurisée)
SECRET_KEY=your-secret-key-here

# Configuration OAuth Google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Configuration OAuth GitHub  
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Configuration SMTP (pour l'envoi d'emails)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password-16-chars-no-spaces

# Configuration PostgreSQL (optionnel - par défaut SQLite)
POSTGRES_USER=suprss_user
POSTGRES_PASSWORD=suprss_pass
POSTGRES_DB=suprss_db
# DATABASE_URL=postgresql://user:password@localhost:5432/suprss_db

# ===== SÉCURITÉ AVANCÉE =====
# Décommentez et configurez selon vos besoins

# Activation de la sécurité avancée
# ENABLE_ADVANCED_SECURITY=true

# Géolocalisation - Pays autorisés (codes ISO séparés par des virgules)
# ALLOWED_COUNTRIES=FR,BE,CH,CA,US

# Géolocalisation - Pays bloqués (codes ISO séparés par des virgules)  
# BLOCKED_COUNTRIES=CN,RU,KP,IR

# Protection contre les tentatives répétées
# MAX_FAILED_ATTEMPTS=5
# LOCKOUT_DURATION_MINUTES=15

# Protection CSRF
# ENABLE_CSRF_PROTECTION=true